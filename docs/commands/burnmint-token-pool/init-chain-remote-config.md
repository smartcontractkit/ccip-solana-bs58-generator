---
type: command
program: burnmint-token-pool
alias: bm
instruction: init-chain-remote-config
read_only: false
execute_capable: true
---

# bm init-chain-remote-config

Create the chain config account for one remote chain: its token address, that token's decimals, and an initially empty remote pool list.

**When to use:** First-time configuration of a remote chain on the pool. Remote pool addresses MUST be empty at init; append them afterwards with append-remote-pool-addresses.

## Invocation

```bash
cct-solana-tx bm --instruction init-chain-remote-config --env devnet [flags]
```

## Flags

| Flag | Arg | Required | Description |
| --- | --- | --- | --- |
| `--program-id` | programId | yes | Burnmint token pool program ID (required for all instructions) |
| `--mint` | mint | yes | Token mint address (required for all instructions) |
| `--authority` | authority | yes | Authority public key (required for all instructions) |
| `--pool-addresses` | json | yes | JSON array of pool addresses for init-chain-remote-config and edit-chain-remote-config (init must be empty; if omitted on edit, existing addresses will be cleared) |
| `--token-address` | address | yes | Remote token address (destination chain) as hex string (required for init/edit-chain-remote-config) |
| `--decimals` | decimals | yes | Remote token decimals 0-255 (destination chain, required for init/edit-chain-remote-config) |
| `--remote-chain-selector` | selector | yes | Remote chain selector (required for set-chain-rate-limit, init-chain-remote-config, edit-chain-remote-config, append-remote-pool-addresses, delete-chain-config and get-chain-config) |

## Failure modes

Passing a non-empty --pool-addresses at init is rejected. See [gotchas](../../gotchas/index.md#init-pool-addresses-empty).

## Source

- Router/flags: [`src/commands/burnmint/index.ts`](../../../src/commands/burnmint/index.ts)
- Handler: [`src/commands/burnmint/init-chain-remote-config.ts`](../../../src/commands/burnmint/init-chain-remote-config.ts)

_Generated by `script/docs/gen-catalog.mjs` from the commander source; do not edit by hand._
