---
type: command
program: lockrelease-token-pool
alias: lr
instruction: set-chain-rate-limit
read_only: false
execute_capable: true
---

# lr set-chain-rate-limit

Set the inbound and outbound token-bucket rate limits for one remote chain.

**Preconditions:** Capacity and rate are in the LOCAL token's smallest units: multiply by 10^decimals of the mint on this chain, not the remote one. The chain must already be configured with init-chain-remote-config.

## Invocation

```bash
cct-solana-tx lr --instruction set-chain-rate-limit --env devnet [flags]
```

## Flags

| Flag | Arg | Required | Description |
| --- | --- | --- | --- |
| `--program-id` | programId | yes | Lockrelease token pool program ID (required for all instructions) |
| `--mint` | mint | yes | Token mint address (required for all instructions) |
| `--authority` | authority | yes | Authority address (required for all instructions) |
| `--remote-chain-selector` | selector | yes | Remote chain selector (required for set-chain-rate-limit, init-chain-remote-config, edit-chain-remote-config, append-remote-pool-addresses, delete-chain-config and get-chain-config) |
| `--inbound-enabled` | enabled | yes | Enable inbound rate limiting (true/false) (required for set-chain-rate-limit) |
| `--inbound-capacity` | capacity | yes | Inbound rate limit capacity (required for set-chain-rate-limit) |
| `--inbound-rate` | rate | yes | Inbound rate limit refill rate per second (required for set-chain-rate-limit) |
| `--outbound-enabled` | enabled | yes | Enable outbound rate limiting (true/false) (required for set-chain-rate-limit) |
| `--outbound-capacity` | capacity | yes | Outbound rate limit capacity (required for set-chain-rate-limit) |
| `--outbound-rate` | rate | yes | Outbound rate limit refill rate per second (required for set-chain-rate-limit) |

## Source

- Router/flags: [`src/commands/lockrelease/index.ts`](../../../src/commands/lockrelease/index.ts)
- Handler: [`src/commands/lockrelease/set-chain-rate-limit.ts`](../../../src/commands/lockrelease/set-chain-rate-limit.ts)

_Generated by `script/docs/gen-catalog.mjs` from the commander source; do not edit by hand._
