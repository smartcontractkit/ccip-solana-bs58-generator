---
type: command
program: lockrelease-token-pool
alias: lr
instruction: configure-allow-list
read_only: false
execute_capable: true
---

# lr configure-allow-list

Enable or disable the allow list and add sender addresses to it. The list gates OUTBOUND transfers only.

## Invocation

```bash
cct-solana-tx lr --instruction configure-allow-list --env devnet [flags]
```

## Flags

| Flag | Arg | Required | Description |
| --- | --- | --- | --- |
| `--program-id` | programId | yes | Lockrelease token pool program ID (required for all instructions) |
| `--mint` | mint | yes | Token mint address (required for all instructions) |
| `--authority` | authority | yes | Authority address (required for all instructions) |
| `--add` | json | yes | JSON array of addresses to add to allow list (required for configure-allow-list) |
| `--enabled` | enabled | yes | Enable allow list (true/false) (required for configure-allow-list) |

## Failure modes

Adding an address already present reverts AllowlistKeyAlreadyExisted (6010). Enabled with an empty list blocks every sender (InvalidSender 6006) rather than allowing all. See docs/gotchas.

## Source

- Router/flags: [`src/commands/lockrelease/index.ts`](../../../src/commands/lockrelease/index.ts)
- Handler: [`src/commands/lockrelease/configure-allow-list.ts`](../../../src/commands/lockrelease/configure-allow-list.ts)

_Generated by `script/docs/gen-catalog.mjs` from the commander source; do not edit by hand._
